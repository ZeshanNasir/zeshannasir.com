# Operating System Files
.DS_Store
.DS_Store?
._*
.Spotlight-V100
.Trashes
ehthumbs.db
Thumbs.db

# Node / Scratch
node_modules/
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnpm-debug.log*

# Secrets & Local Environment
*.pem
*.key
*.token
*.local
credentials.json
tunnel.json
.env

# Temporary artifacts
tmp/
scratch/

# Source content contains private infrastructure details (IPs, container IDs)
# not suitable for public repositories. Keep locally, exclude from Git.
docs/source-content/

# Config templates contain private IPs and a rejected Cloudflare Tunnel architecture.
# Keep locally for reference, exclude from Git.
config/
